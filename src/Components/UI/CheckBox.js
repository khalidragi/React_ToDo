import React from 'react';
import { Checkbox, Segment } from 'semantic-ui-react';

const Checkbox = () => (
  <div>
    <Segment compact>
      <Checkbox />
    </Segment>
  </div>
);

export default Checkbox;
